Finding my perl and bash coding has left me not knowing how to do POSIX
shell input validation. I'm thinking `sed` (but I so hate it). But `sed`
is the only reliable POSIX thing (and it's on BusyBox).
